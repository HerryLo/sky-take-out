<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liuheng.mapper.DishMapper">
    <select id="countByCategoryId" resultType="java.lang.Integer">
        select count(*) from dish where category_id = #{id}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into dish
        (name, value, categoryId, price, image, description, status, create_time, update_time, create_user, update_user)
        values
            (#{name}, #{value}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
</mapper>