<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liuheng.mapper.SetmealMapper">
    <select id="countByCategoryId" resultType="java.lang.Integer">
        select count(*) from setmeal where category_id = #{id}
    </select>

    <!-- 新增套餐 -->
    <insert id="save" parameterType="com.liuheng.entity.Setmeal" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal
        (category_id, name, price, status, description, image, create_time, update_time, create_user, update_user)
        values
            (#{categoryId}, #{name}, #{price}, #{status}, #{description}, #{image}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!-- 修改套餐 -->
    <update id="update" parameterType="com.liuheng.entity.Setmeal">
        update setmeal
        <set>
            category_id = #{categoryId},
            name = #{name},
            price = #{price},
            status = #{status},
            description = #{description},
            image = #{image},
            update_time = #{updateTime},
            update_user = #{updateUser}
        </set>
        where id = #{id}
    </update>

    <!-- 根据套餐ID删除关联的菜品 -->
    <delete id="deleteBySetmealId" parameterType="java.lang.Long">
        delete from setmeal_dish where setmeal_id = #{setmealId}
    </delete>

    <!-- 根据ID删除套餐 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        delete from setmeal where id = #{id}
    </delete>
</mapper>